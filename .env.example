# Vercel Blob — create a Blob store in the Vercel dashboard and link it to this project.
# After linking, Vercel injects BLOB_READ_WRITE_TOKEN in production and `vercel dev` locally.
BLOB_READ_WRITE_TOKEN=
